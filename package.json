{
  "name": "liberty-talking-patient",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser talking-head patient avatar — lip-sync feasibility spike",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "fetch-avatar": "node scripts/fetch-avatar.mjs",
    "test": "node --test \"tests/**/*.test.js\"",
    "deploy": "npm run build && gh-pages -d dist -m \"Deploy\""
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^8.2.1"
  }
}
